import type { Page } from 'puppeteer';
import { subscriptionType } from './type';
/**
 * Solve captchas using `nocaptchaai.com` API service
 *
 * @param page - Puppeteer page instance
 * @param apiKey - API key
 * @param uid - UID
 * @param type - `free` or `pro`
 * @param debug - true or false
 */
export declare const solveCaptcha: (page: Page, apiKey: string, uid: string, type: subscriptionType, debug: boolean) => Promise<void>;
